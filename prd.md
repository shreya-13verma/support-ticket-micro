# Support Ticket Raiser — PRD

## 1. Overview
A support ticket raising and tracking system for a small support desk (10–20 concurrent agents/customers), built as **two independent FastAPI microservices** — `user_service` (identity/auth/roles) and `assign_service` (tickets/assignment/SLA/reporting) — with a React + TypeScript frontend.
